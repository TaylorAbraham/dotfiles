# Make vim pretty
set -g default-terminal "screen-256color"

# Supports vim-tmux-clipboard which shares Vim clipboards in tmux
set -g focus-events on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

